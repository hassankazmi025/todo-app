const inputItem = document.querySelector("#add-todo");
const addBtn = document.querySelector(".selec-btn");

addBtn.addEventListener("click", (e) => {
  e.preventDefault();
  const newValue = inputItem.value;
  
});

function createLiElement(){
    
}